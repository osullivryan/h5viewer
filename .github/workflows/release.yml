name: Release

# Publishes to the JetBrains Marketplace when a version tag is pushed (e.g. v0.1.0).
# Required repository secret:
#   PUBLISH_TOKEN - Marketplace permanent token (https://plugins.jetbrains.com/author/me/tokens)
# The plugin is published unsigned. See RELEASING.md.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  release:
    name: Verify & publish
    runs-on: ubuntu-latest
    permissions:
      contents: write # attach the built zip to the GitHub Release
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run the IntelliJ Plugin Verifier
        run: ./gradlew verifyPlugin

      - name: Build plugin
        run: ./gradlew buildPlugin

      - name: Build HDF5 JDBC driver
        run: ./gradlew :jdbc:shadowJar

      # Create the GitHub Release (plugin zip + standalone JDBC driver) BEFORE the
      # Marketplace publish, so the release assets are produced even if publishing
      # fails — notably the first-ever version, which must clear manual moderation
      # in the Marketplace web UI before automated publishPlugin is accepted.
      - name: Attach plugin zip and JDBC driver to the GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/distributions/*.zip
            jdbc/build/libs/*-all.jar
          generate_release_notes: true

      # A re-run or a tag on an already-published version should not fail the release.
      # Tolerate only the "version already on the Marketplace" case; any other publish
      # failure (bad token, network, verification) still fails the job.
      - name: Publish to JetBrains Marketplace
        env:
          PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
        run: |
          if ./gradlew publishPlugin 2>&1 | tee publish.log; then
            exit 0
          fi
          if grep -q "already contains version" publish.log; then
            echo "::notice::This version is already on the JetBrains Marketplace — nothing to publish."
            exit 0
          fi
          echo "::error::publishPlugin failed for a reason other than a duplicate version."
          exit 1
